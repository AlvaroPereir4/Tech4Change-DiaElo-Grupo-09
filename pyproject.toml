[project]
name = "diaelo"
version = "0.1.0"
description = "Dados que viram cuidado, nao diagnostico."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
